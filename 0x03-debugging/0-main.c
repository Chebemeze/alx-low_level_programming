#include "main.h"

/**
 * main - checks fucntion to se if it produces the right result
 * when given a value of 0.
 * Return: returns 0 if successful.
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
