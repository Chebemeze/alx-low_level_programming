#include "main.h"

/**
 * add - adds two integers
 * @a: one of the inputed integer
 * @c: the second inputed integer
 * Return: returns the sum of the two integers.
 */

int add(int c, int a)
{
	int result = c + a;

	return (result);
}
