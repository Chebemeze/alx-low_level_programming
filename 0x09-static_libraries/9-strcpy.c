#include "main.h"

/**
 * *_strcpy - A function that copies a string from pointer to another pointer
 * @dest: copies the string from src
 * @src:pointer that copies the string
 * Return: pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
	return (0);
}
