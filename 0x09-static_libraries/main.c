#include "main.h"

/**
 * main - checks if function is working properly
 * Return: 0 if successful.
 */

int main(void)
{
	_puts("\"At the end of the day, my goal was to be the best hacker\"\n\t- Kevin Mitnick");
	return (0);
}
