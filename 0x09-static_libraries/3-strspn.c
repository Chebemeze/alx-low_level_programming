#include "main.h"

/**
 * _strspn - gets the length of a prefix substring
 * @s: First array given
 * @accept: Second array given
 * Return: Returns the number of bytes
 */

unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
