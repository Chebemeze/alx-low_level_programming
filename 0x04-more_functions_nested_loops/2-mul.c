#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: first integer given.
 * @b: second integer given.
 * Return: returns the result if the function is successful.
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
